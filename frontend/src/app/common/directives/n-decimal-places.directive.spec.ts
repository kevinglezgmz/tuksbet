import { NDecimalPlacesDirective } from './n-decimal-places.directive';

describe('NDecimalPlacesDirective', () => {
  it('should create an instance', () => {
    const directive = new NDecimalPlacesDirective();
    expect(directive).toBeTruthy();
  });
});
